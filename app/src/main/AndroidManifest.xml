<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="et.tsingtaopad">

    <application
        android:name=".home.app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAppTheme">

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="et.tsingtaopad.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

        <!--<activity android:name=".dd.ddxt.shopvisit.XtVisitShopActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>-->
        <activity android:name=".home.login.LoginActivity"
                  android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--协同终端 主Activity-->
        <activity android:name="et.tsingtaopad.home.app.MainActivity"
                  android:screenOrientation="portrait"/>

        <!--协同终端 主Activity-->
        <activity android:name=".dd.ddxt.shopvisit.XtVisitShopActivity"
                  android:screenOrientation="portrait"/>
        <!--追溯终端 主Activity-->
        <activity android:name=".dd.ddzs.zsshopvisit.ZsVisitShopActivity"
                  android:screenOrientation="portrait"/>
        <!--拍照图片预览-->
        <activity android:name=".dd.ddxt.camera.XtShowPicActivity"
                  android:screenOrientation="portrait"/>

        <!-- 启动应用的时候开启servic 主要负责监听网络变化 执行上传功能 -->
        <service android:name=".home.app.AutoUpService" />

    </application>

</manifest>